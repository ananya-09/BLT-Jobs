{
  "name": "blt-jobs",
  "private": true,
  "scripts": {
    "build:jobs": "python3 scripts/build_jobs.py",
    "validate:jobs": "python3 scripts/validate_jobs.py",
    "list:jobs": "python3 scripts/list_jobs_status.py"
  }
}
